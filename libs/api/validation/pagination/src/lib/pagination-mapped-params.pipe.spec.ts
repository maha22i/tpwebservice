import { PaginationMappedParamsPipe } from './pagination-mapped-params.pipe';

describe('PaginationMappedParamsPipe', () => {
  it('should be defined', () => {
    expect(new PaginationMappedParamsPipe()).toBeDefined();
  });
});
